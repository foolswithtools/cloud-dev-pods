// Phase 4 helper: validate config.yaml against the Zod schema and print the
// fully resolved config (defaults filled in). Used as a dry-run for
// `cdk synth` debugging.
//
// Usage:
//   tsx scripts/render-config.ts [path/to/config.yaml]

export {};
