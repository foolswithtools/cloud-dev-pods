# GUI setup walkthrough

Phase 11 will populate this with screenshot-driven instructions for users who prefer point-and-click over CLI:

- Creating the GitHub OAuth App in the GitHub web UI.
- Configuring repo Variables and Environment Secrets in the GitHub Settings UI.
- Verifying AWS Console post-conditions after `bootstrap-aws.yml`, `cluster-up.yml`, and `pod-up.yml`.

For now, prefer [`setup-cli.md`](setup-cli.md).
