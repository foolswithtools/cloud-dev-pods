name: ci-security
# Phase 3: gitleaks, trivy fs scan on runtime/, osv-scanner.

on:
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 12 * * *"

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - run: echo "Phase 3 — wire up gitleaks, trivy, osv-scanner."
