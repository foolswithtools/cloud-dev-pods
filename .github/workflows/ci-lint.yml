name: ci-lint
# Phase 3: actionlint, markdownlint, eslint, shellcheck, lychee link-check.

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - run: echo "Phase 3 — wire up actionlint, markdownlint, eslint, shellcheck, lychee."
